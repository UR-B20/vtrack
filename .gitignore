# dependencies
node_modules/
.pnpm-store/

# build output
dist/
dev-dist/
*.tsbuildinfo

# environment — never commit secrets (CLAUDE.md §1)
.env
.env.*
!.env.example

# python (services/engine)
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# test / tooling
coverage/
.vitest/

# editors / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Gate photos for the M2 benchmark: personal data (PDPA), and this repo is public. They stay
# on the owner's laptop; only aggregate results are committed (CLAUDE.md §11).
services/engine/tests/plates/
